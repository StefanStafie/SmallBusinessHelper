<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:ScheduleAndStockManagement.Controls"
    x:Class="ScheduleAndStockManagement.Views.InventoryItemTypeEditPage"
    Title="Edit Inventory Item">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="14">

            <Label Text="Name" FontAttributes="Bold"/>
            <Entry Text="{Binding Name}" 
                   Placeholder="Enter item name"/>

            <Label Text="Appointment Type" FontAttributes="Bold"/>
            <Picker ItemsSource="{Binding PossibleAppointmentTypes}"
                    SelectedItem="{Binding SelectedAppointmentType}"
                    Title="Select appointment type"/>

            <Label Text="Designation" FontAttributes="Bold"/>
            <controls:InventoryTypeSelector SelectedInventoryType="{Binding SelectedInventoryType}" />

            <Button Text="Save"
                    Clicked="OnSaveClicked"/>

            <Button Text="Delete"
                    Clicked="OnDeleteClicked"
                    BackgroundColor="{StaticResource DangerColor}"
                    TextColor="White"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
